To compile:

Edit "Makefile", replace LLVM_DIR with a pointer to your llvm dir

From this folder (clang_example) run - 
$ make

Then to run on a source file:

clang -cc1 -load CreateConstraints.so -plugin CreateConstraints ~/file.c
clang -cc1 -load PointerAnalysis.so -plugin CreateConstraints ~/file.c

